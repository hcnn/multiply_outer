#ifndef MULTIPLY_OUTER_H
#define MULTIPLY_OUTER_H

#include <stddef.h>
#include "rowidx/rowidx.h"

void multiply_outer(const double*, const double*, double*, size_t, size_t);

#endif /* MULTIPLY_OUTER_H */
